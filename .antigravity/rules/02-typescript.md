# TypeScript Rules

- TypeScript is mandatory.
- Strict mode is mandatory.
- Avoid `any`.
- Prefer discriminated unions for job, queue, and error states.
- Validate external and infrastructure input at boundaries.
- Model domain concepts explicitly.
