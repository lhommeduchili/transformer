# React Rules

- React components must not contain business logic.
- Components call application use cases and render application state.
- Keep feature UI close to feature modules.
- Accessibility behavior is required.
- Do not call FFmpeg, workers, or File System Access API directly from components.
