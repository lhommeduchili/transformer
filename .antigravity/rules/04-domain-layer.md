# Domain Layer Rules

The domain layer owns entities, value objects, policies, compatibility validation, filename sanitization rules, queue rules, error classification, and domain events.

It must not import React, Zustand, browser APIs, workers, FFmpeg, or infrastructure modules.
