# Infrastructure Layer Rules

The infrastructure layer implements ports for FFmpeg.wasm, workers, browser file APIs, metadata parsing, logging, storage, and report export.

Translate browser-specific errors and implementation details before passing results to application services.
